
def brand(vehicle, put_brand):
    vehicle.brand = put_brand   # put_brand after the object name 'vehicle' is what's called the function in main


def color(vehicle, put_color):
    vehicle.color = put_color   # put_color after the object name 'vehicle' is what's called the function in main


def country_origin(vehicle, put_origin_country):
    vehicle.origin_country = put_origin_country  # origin_country.object name 'vehicle' is what's  called the function
